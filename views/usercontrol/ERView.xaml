<UserControl x:Class="NolMed.views.usercontrol.ERView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NolMed.views.usercontrol"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBlock Text="Emergency center" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="20,30,0,0" FontSize="24"/>
        
        <Grid Margin="20,67,20,20">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{Binding EmergencyRooms}">

                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Width="200" Height="120"
                                    Background="{Binding BackgroundColor}"
                                    Margin="10"
                                    Command="{Binding DataContext.RoomBoxClicked, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}">

                                <Grid Width="200" Height="120">
                                    <TextBlock Text="{Binding RoomNumber}" 
                                               Foreground="White" 
                                               FontSize="16"
                                               HorizontalAlignment="Left" 
                                               VerticalAlignment="Top"
                                               Margin="10"/>
                                    <TextBlock Text="{Binding RoomName}"
                                               Foreground="White" 
                                               FontSize="16"
                                               HorizontalAlignment="Center" 
                                               VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding PatientName}"
                                               Foreground="White" 
                                               FontSize="16"
                                               HorizontalAlignment="Center" 
                                               VerticalAlignment="Center"
                                               Margin="0,35,0,0"/>
                                </Grid>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>

                </ItemsControl>

            </ScrollViewer>
        </Grid>
        
    </Grid>
</UserControl>
