<UserControl x:Class="NolMed.views.usercontrol.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NolMed.views.usercontrol"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBlock Text="Welcome to the dashboard" 
                    FontSize="24" 
                    HorizontalAlignment="Left" 
                    VerticalAlignment="Top" Margin="10,10,0,0"/>
        
        <Grid Width="200"
              HorizontalAlignment="Left">

            <ListBox ItemsSource="{Binding AllMenuItems}"
                     SelectedItem="{Binding SelectedItem}"
                     Background="Transparent" BorderThickness="0" Margin="0,51,0,0">

                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="20,15"/>
                        <Setter Property="Margin" Value="5,2"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Cursor" Value="Hand"/>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Title}" Foreground="Black" Padding="10" FontSize="16"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

        </Grid>
        <Grid Margin="200,47,0,0">
            <ContentControl Content="{Binding CurrentView}"/>
        </Grid>
    </Grid>
</UserControl>
