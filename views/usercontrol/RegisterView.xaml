<UserControl x:Class="NolMed.views.usercontrol.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NolMed.views.usercontrol"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Width="300" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Register" FontSize="20" Margin="0,0,0,20" HorizontalAlignment="Center"/>

            <TextBlock Text="First name:" />
            <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" Padding="5"/>
            
            <TextBlock Text="Last name:" />
            <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" Padding="5"/>
            
            <TextBlock Text="Username:" />
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" Padding="5"/>

            <TextBlock Text="Password:" />
            <PasswordBox x:Name="UserPasswordBox" Margin="0,0,0,20" Padding="5"/>

            <Button Content="Register" 
                    Command="{Binding RegisterCommand}" 
                    CommandParameter="{Binding ElementName=UserPasswordBox}" 
                    Padding="10" Background="#3498DB" Foreground="White" FontSize="16"/>

            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,10,0,0" HorizontalAlignment="Center"/>
        </StackPanel>

        <Button Content="Login"
                Command="{Binding LoginCommand}"
                Width="60" Height="25"
                Padding="5" Background="Transparent" Foreground="Black" FontSize="10" Margin="490,371,250,54" BorderThickness="0"/>
    </Grid>
</UserControl>
